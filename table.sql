CREATE TABLE list(id SERIAL PRIMARY KEY, site text,names text);

CREATE TABLE videos(id SERIAL PRIMARY KEY,name text,title text,link text,pubdate text, content text, contentsnippet text, guid text, isoDate text, url text);
